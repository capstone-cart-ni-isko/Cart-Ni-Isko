{
    "name": "cart-ni-isko",
    "private": true,
    "scripts": {
        "dev": "backend\\node_modules\\.bin\\concurrently.cmd -k -n api,web -c blue,green \"npm --prefix backend run serve\" \"npm --prefix frontend run dev\"",
        "dev:api": "npm --prefix backend run serve",
        "dev:web": "npm --prefix frontend run dev",
        "test": "npm --prefix backend run test && npm --prefix frontend run test",
        "build": "npm --prefix frontend run build"
    }
}
